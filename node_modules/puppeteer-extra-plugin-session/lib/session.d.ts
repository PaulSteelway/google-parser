import { Page } from 'puppeteer';
import { StorageProvider, StorageProviderName } from './classes/storage-provider';
import { SessionData } from './types/session-data';
export declare const storageProviderMap: Record<StorageProviderName, StorageProvider>;
export declare function getSessionData(page: Page, providers?: StorageProviderName[]): Promise<SessionData>;
export declare function setSessionData(page: Page, sessionData: SessionData, providers?: StorageProviderName[]): Promise<void>;
